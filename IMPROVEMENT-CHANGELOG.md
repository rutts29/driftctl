# Improvement changelog

This changelog records the experiments that shaped the standalone hackathon implementation. Discarded experiments remain part of the story rather than being counted as evidence.

## Current product correction

- Entries through `Private-release safety gate` describe a useful checkpoint/recovery prototype, not the intended continuous same-session keeper.
- Product drift: fork isolation and evaluation controls replaced the original runtime invariant in `SPEC.md`, `ARCHITECTURE.md`, and completion gates.
- Recovery decision: retain the importer, semantic history, projection, conflict/goal state, private storage, verification, packaging, tests, and evidence; replace the top-level runtime with exact-session Codex lifecycle hooks.
- New closure rule: no ongoing-session claim until an installed hook injects validated intent into the same attached session across prompt, restart/resume, compaction, conflict, and detach.

| Stage | What changed and why | Evidence | Decision |
|---|---|---|---|
| Native-goal race removal | Final Daybreak review found a TOCTOU race in automatic same-session `get → clear → set`: Codex 0.150.1 exposes no revision or conditional mutation field, so a concurrent operator goal could be overwritten. | Generated local App Server schemas confirm clear accepts only `threadId` and set has no expected revision. A process regression changes the goal between read-backs; Driftctl exits `2`, preserves the concurrent goal and ledger, and emits no clear/set RPC. Exact operator-set goal confirmation later commits using read-only checks. | Removed the same-session mutation/rollback API. Goal changes are operator-executed inside the exact Codex session; Driftctl only verifies and commits. |
| Product-drift recovery | The project shipped `inspect → isolated child → verify`, but no worker stayed attached after the initial injection. The current conversation reproduced the exact failure: the original ongoing-ledger requirement remained stable while later implementation constraints became a replacement goal. | The pre-correction source had no enrollment lifecycle or prompt/compaction hooks; a continued child could drift after one-shot grounding. Codex 0.150.1 exposes stable lifecycle hooks for prompt, stop, session start, and compaction. | Kept the prototype as recovery/evaluation support and replaced the primary runtime with the exact-session hook flow. |
| Installed same-session boundary | Added an isolated Codex plugin, exact session enrollment, strict unenrolled no-op, durable prompt receipts, semantic fold, validation, commit, and active-intent injection. | The real plugin loaded in an isolated Codex home. An exact persisted session attached, while an unknown session created no enrollment and a same-repository control received no context. Nine hook process cases pass. | Kept. The plugin is opt-in and local; existing Codex config, hooks, repository guidance, authentication, and other sessions remain user-owned. |
| Explicit in-session control | External session IDs made the working keeper awkward to activate and easy to misconfigure. Added an explicit-only `$driftctl` skill; its literal `on`, `off`, and `status` prompts are handled by the lifecycle hook using Codex's exact session ID. | Process tests cover exact activation, near-match rejection, inert lifecycle events, read-only status, same-repository isolation, detach, and control-message source accounting. A checksum-installed binary materialized the plugin in a disposable marketplace and completed on/status/off without touching the active Codex profile. | Kept. Installation alone is inert. The CLI remains diagnostic compatibility; normal activation requires one explicit in-session command. |
| False goal-change classification | A real resumed diagnostic prompt and then a session-wide `DRIFT_MARKER` output constraint were both proposed as overall goal replacements by Luna. Driftctl blocked before the model, but the classification was wrong. | Both pending proposals and zero-token blocked turns were retained. The incremental prompt now permits `goal_change` only for explicit replacement of the overall goal/objective/task and explicitly treats `from now on`, style, output, tool, scope, validation, and stop conditions as ordinary steering. Reject/edit/approve process tests prove that non-approval cannot mutate the native goal. | Kept fail-closed approval. Do not claim that Luna's semantic classification is infallible. |
| Validator-directed repair | After classification became additive, Luna twice filled fields that are illegal for an `add`; an earlier conflict repair also repeated an invalid shape because it received only a generic repair label. | Real attempts remained blocked with the accepted projection unchanged. Repair now receives the exact validator code, and the prompt states legal add, supersede, and conflict shapes. Focused semantic tests pass. | Kept one bounded repair. No review/fix loop or validator weakening was added. |
| Hook/provider identity split | Real Codex assigned a different persisted `userMessage` ID from the `UserPromptSubmit` hook turn ID. Exact-ID receipts therefore caused the same prompt to be classified again at `Stop`. | A red process test reproduced different IDs. Receipts now use a content-only prompt digest inside the exact enrolled session and bind once to the later provider record ID. The test and full hook process suite pass; the real receipt shows distinct hook/provider IDs with one binding. | Kept the separate hook/provider cursor model. Later identical prompts cannot reuse a consumed receipt. |
| Same-session restart and compact acceptance | Attached a fresh persisted session, added the marker constraint through the trusted installed hook, then resumed the same session from a separate Codex process without repeating the constraint. | First reply: `Understood. DRIFT_MARKER`. Separate-process reply restated the active ledger and ended in `DRIFT_MARKER`. The projection advanced from revision 4 to 5 and retained the source-linked constraint. A production-shaped real `PreCompact → SessionStart(compact)` hook run restored the same goal and marker with zero conflicts. | Passed the MVP behavior boundary. This is product-function evidence, not a statistical coding-efficacy result. |
| Baseline | A fresh general-purpose Codex agent resumed from the surviving worktree after a hard transcript loss. | Final baseline: 3/5 protected cases verified; two failed agents still emitted completed turns. | Kept as the narrowly labelled worktree-only recovery baseline. |
| Ledger core | Added append-only goal, requirement, steering, evidence, and closure events. | Reopen tests preserve steering and closure exits `2` while requirements remain unresolved. | Kept. |
| Codex adapter | Reconstructed the goal/frontier for a fresh ephemeral Codex process and retained raw JSONL trajectories. | Real smoke run and adapter tests completed without changing `AGENTS.md`, `CLAUDE.md`, or the ledger. | Kept as the first convenience adapter; generic integration remains the JSON contract. |
| Visible injected grader experiment | Initially copied the late integration test into the agent's writable workspace before the second turn. | Both modes passed; agents could read and even edit the supposed grader, so the result did not test continuity. | Removed from scoring. Graders now remain external and are fingerprinted before and after runs. |
| Preserved constraints | Included satisfied requirements in later agent context, not only the unresolved frontier. | Adapter test proves a satisfied requirement remains present as a constraint to preserve. | Kept. |
| Ambiguous rename experiment | The first rename grader silently expected payload methods that the written requirement did not name. | The workflow recovered the intent but implemented a different reasonable public API; both grader outcomes were unusable. | Discarded. Made `submit_payload` and `resume_payload` explicit, then reran both modes on a new fingerprint. |
| Final five-case pass | Combined durable recovery, external graders, evidence-gated closure, and deterministic scoring. | Workflow 5/5 versus baseline 3/5; premature completion 0 versus 2. Mean time increased 10.5% and total tokens increased 13.5%. | Kept as first-pass evidence, with one trial per case and the hard-loss scope stated prominently. |
| Doubt-driven pilot review | Tested whether the five-case comparison supports a long-session reliability claim. | The baseline never received late steering; three cases tied; two favored the workflow; one run per arm; fixtures were small. | Relabelled as hard-loss fault-injection evidence; no native-resume, long-horizon, or statistical-significance claim. |
| Long-session pivot | Changed the target from replacing lost history to re-grounding an intact native session with a bounded active-intent projection. | Codex CLI exposes native session forking; critical review requires identical checkpoint branches for behavioral comparison. | Use native history in both arms; retain immutable history; compact only the active prompt projection; treat retrospective inspection as fidelity evidence only. |
| Summary reproducibility check | Compared fresh scorer output with the published summary before committing the pilot package. | `primary_metric` and `by_mode` matched; exact JSON differed because the summary adds design and caveat metadata and omits per-case rows. | Kept the enriched report and documented the reproducible metric boundary instead of claiming byte-identical output. |
| Codex session intake | Added a read-only App Server importer for `inspect codex`; it selects only the canonical current-directory session, extracts explicit user text only, and emits a redacted blocking report until projection exists. | Five subprocess protocol tests cover selection, pagination, malformed/truncated responses, redaction, and source-worktree non-mutation. A fresh disposable read-only Codex session was initially not discoverable immediately after `codex exec`; after App Server materialization, the shipped CLI imported it with exit `2` and the source worktree unchanged. | Kept. The command fails closed when no session is yet visible; the reproduction must wait for the provider session to materialize rather than claim immediate availability. |
| Codex child isolation spike | Forked one disposable persisted source session into two real child worktrees at the same commit, then resumed the persistent child. | Child IDs and workdirs were distinct; source-session SHA-256 and source Git state matched before/after. The ephemeral child had no stored transcript and could not resume. | Use persisted children for resumable work. Comparison children may be disposable, but are persisted when native goal control is required. |
| Native goal spike | Exercised App Server goal get/clear/set/read-back on a real disposable fork rather than sending interactive slash commands. | Exact read-back passed and the parent goal was unchanged. Ephemeral goal calls failed; forked children began with no inherited goal. | Use persisted children, seed goal state explicitly, and capability-probe the versioned App Server boundary. Keep manual child handoff as fallback. |
| Semantic inspect slice | Replaced the import-only `projection_not_built` result with a pinned Codex compactor, deterministic source/transition validation, v2 History replay, and bounded sanitized projection output. | A shipped-binary run inspected a real three-turn persisted session with additive steering and explicit supersession using Luna Max. The first proposal failed local validation; the single permitted repair produced a usable projection and exit `0`. The earlier clause remained in history as `superseded`; source App Server readback and source Git state matched before/after. Observed resolver overhead was 2 calls, 156,565 ms, 27,321 input tokens, 12,407 output tokens, and 11,126 reasoning-output tokens. A disclosure-timing rerun then produced two shape-invalid proposals and correctly exited `1`; a diagnostic rerun again repaired one shape-invalid proposal and succeeded. A later final run exhausted its repair on two shape-invalid proposals and exited `1`. Failure-driven instrumentation now retains each raw JSONL trajectory and final structured message in private local artifacts with `0700` directory and `0600` file modes while exposing only opaque artifact IDs. The next shipped-binary run succeeded in one call and 86,434 ms with 13,658 input, 6,879 output, and 6,214 reasoning-output tokens; its retained structure matched the schema exactly. Every run preserved source session and Git state, and disclosure was observable before provider completion. | Kept. Model output remains proposal-only, repair remains capped at one, and unresolved ordinary frontier does not block inspect. The observed repairs, repair-exhaustion failures, nondeterminism, latency, and token overhead are retained rather than presented as deterministic success. |
| Durable v2 run state | Added XDG-local immutable history segments, a pending delta, atomic projection replacement, recovery, symlink refusal, and one-writer enforcement so accepted intent can survive interruption. | The first create-new sentinel lock survived a killed writer and made the run permanently busy. Replacing it with an OS-released advisory lock made a live second writer fail deterministically while the same store reopened after forcibly killing and reaping the holder. Twelve real-filesystem tests pass, including interrupted projection rotation and immutable-history checks; the merged suite passes formatting, strict Clippy, and all 53 tests. | Kept the storage layer and the crash regression. This does not close the product checkpoint until `inspect` creates and reopens it through the shipped CLI. |
| Persisted incremental inspect | Wired source cursors, projection-plus-delta resolution, source recheck, cached reuse, and sanitized `bundle --run` export through the shipped CLI. | The first fresh-XDG run failed because the state parent did not exist; the next rejected valid resolver metadata as semantic drift; the next exposed `0755` state directories. Recursive state creation, semantic-only projection comparison, and `0700` directories/`0600` files fixed those failures. A process-boundary test now runs initial inspect, zero-call cached inspect, bundle export, one-record incremental inspect whose prompt excludes prior raw transcript, and another zero-call reuse while the source Git tree remains unchanged. Formatting, strict Clippy, and all 66 tests pass. | Kept. Incremental semantic state is durable and bounded. Initial full-session import is still one call rather than chronological chunks, and conflict/goal approval remains open. |
| Bounded initial import | Reused the incremental validator to prevent a large first inspection from sending the complete transcript in one prompt. | A synthetic 105 kB three-record session was split into three ordered calls under a 64 KiB record-payload bound. Later prompts contained the accepted projection and only their current record, never earlier raw markers; the CLI disclosed three expected/six maximum calls before invocation. Small sessions retain one call. Formatting, strict Clippy, and all 67 tests pass. | Kept. One source record larger than the bound fails before a provider call instead of being truncated. |
| Bounded child-goal adapter | Added persisted App Server fork and child-only clear/set/read-back operations for an approved objective. | Four fake-process tests cover exact child ID/CWD, persisted-child enforcement, unsupported methods, partial transitions, neutral turn start, and child-only mutation. The first implementation checked the parent only before migration; a new changed-parent scenario failed, so the adapter now reads the parent again and rejects any change. | Kept as a reusable boundary. It is not yet wired through `continue`, has not run through the shipped CLI, and does not close T13/G3. |
| Durable goal-change blocker | Changed explicit overall-objective steering from an automatic revision into a source-linked operator proposal. | The first process test exposed a real failure: the proposal blocked once, then disappeared on cached inspect and exit changed from `2` to `0`. A delegated generic store grew to 1,563 production lines and was discarded. The replacement persists one private atomic proposal bound to source digest, projection revision, and event sequence; cached inspect and sanitized bundle remain blocked with zero extra model calls. A process-boundary fault then replaced `source.json` during the provider call: proposal persistence succeeded, cursor commit failed, and the next run recovered the blocker and cursor with zero additional model calls. Native-goal mismatch also blocks without exposing the parent goal. | Kept the narrow store and both regressions. Approval/edit/reject/cancel and shipped `continue` integration remain open. |
| Operator-gated child continuation | Wired the pending goal decision, equal workspace snapshot, persisted child fork, exact child goal migration, accepted-history revision, and neutral projected turn through `driftctl continue`. | Fake App Server/filesystem E2E first blocks unattended input without forking. Edit and cancel keep the proposal blocked; approval clears/sets/reads back only the child goal, starts its turn, records the approved goal revision, and leaves the parent/source unchanged. Retain rejects the new steering and seeds the child with the prior goal. A run-store lock failure on edit was found and fixed by releasing the local run before cached re-inspection. Compare and continue outputs now also state that isolation is workspace-only and cannot contain inherited host-wide/YOLO permissions. Strict Clippy and all 77 tests pass. | Kept as the first vertical `continue` slice. The child remains isolated and adoption manual. A real App Server run, interrupted approval recovery, unattended preauthorization, and diff/evidence output remain open, so T13/G3 are not complete. |
| Real continuation closure | Ran the release binary on a fresh persisted Luna Max source session instead of treating fake RPC success as completion. | Real `inspect` repaired one `add_irrelevant_fields` proposal and succeeded in 71,249 ms with 2 calls, 27,473 input, 3,584 output, and 2,506 reasoning-output tokens. Live `continue` then exposed three adapter defects in order: clearing an already-absent fork goal returned `false`; a new App Server could not `turn/start` an unloaded child; closing the server after an `inProgress` response persisted the turn as interrupted. Driftctl now skips clear only for a pre-read absent goal, resumes and validates the exact persisted child/CWD, preserves provider error details, and waits for the matching terminal notification without auto-approving requests. The final release run completed the isolated child turn; independent `thread/read` matched the returned child/turn IDs with `completed` and no error. Parent and source checks passed. Four failed disposable children/workspaces remain in private local state as honest failure artifacts. | Kept the failure-driven fixes. This closes the real fork/goal/turn process boundary for the simple no-approval case, not compare, verification, interrupted approved-goal recovery, packaging, or G3. |
| Git-shaped isolated checkpoints | Rechecked whether the successful read-only child workspace was realistic for coding work. It contained the selected files but no `.git`, so normal agent Git commands would fail. | Workspace E2E now requires each equal candidate to have a clean one-commit repository. Driftctl initializes a fresh history-free repository after materializing only the selected tracked/nonignored bytes; dirty and untracked source bytes, modes, and safe symlinks become the clean checkpoint, while ignored state, provider config, credentials, and hidden graders remain absent. All three real-filesystem isolation tests pass. | Kept. This avoids mutating the source `.git` and avoids copying source history. A real coding turn and paired post-run diff are still required. |
| Paired comparison entrypoint | Added `driftctl compare` only after equal Git-shaped candidates and terminal child continuation worked separately. | The subprocess E2E creates two distinct persisted children from one inspected parent and equal manifests. Both inherit the parent worker policy and exact neutral prompt; the baseline receives no extra context and the workflow receives only the sanitized bounded projection. Both terminal statuses and Git changed paths are recorded, parent/source remain unchanged, and adoption is `none`. Strict Clippy and all 79 tests pass. | Kept for a real paired run. Hidden requirement-specific verification is not wired yet, so terminal completion and changed paths are evidence inputs, not verified completion. |
| Real paired process check | Ran the release `compare` entrypoint on the disposable read-only source task. | Starting manifests were equal; two distinct persisted Luna children inherited the same parent policy and neutral prompt; only the workflow input contained the bounded projection. Both turns returned `completed`, both candidate Git diffs were empty, and source/parent checks passed. Independent App Server reads matched both reported thread/turn IDs with terminal `completed` and no errors. The release verifier then ran the same `repository-unchanged` check on both retained candidates: both passed with identical command, verifier, and before/after candidate digests; raw outputs were empty and private artifacts were retained. | Valid process/fairness/verifier smoke pair only. The task was trivial, so it is not evidence that the workflow outperforms baseline. |
| Requirement-specific verifier slice | Replaced the discarded generalized verifier branch with one direct external command bound to one requirement ID. | The shipped `verify` subprocess captures exact command/stdout/stderr only under private state and emits command, verifier, candidate, stdout, and stderr digests plus exit status and timing. Real-process tests pass an external check, distinguish exit `7` from candidate mutation, reject repeated requirement IDs, and prove raw output and local paths stay out of public JSON. Strict Clippy and all 81 tests pass. | Kept as the minimum evidence boundary. Compare/eval orchestration must still invoke it per requirement; no aggregate terminal-status claim is upgraded to verified completion. |
| Failed continuation status | Fault-injected a terminal `failed` child turn through the public `continue` entrypoint. | The first run returned exit `0`, status `started`, and manual adoption despite exposing `turn_status: failed`. The regression now returns exit `2`, `invalid_continuation`, and adoption `none` while retaining the isolated child locator and proving parent/source unchanged. | Kept the narrow status fix; provider completion remains necessary but not sufficient for verified closure. |
| Fresh-prefix installation rehearsal | Installed the crate with `cargo install --locked --path` into a new local prefix, invoked its help entrypoint, then ran the installed `verify` command against a real Git checkpoint with a fresh state root. | Installation and verification passed, candidate digests matched, and raw artifacts were `0600` under `0700` directories. The rehearsal also exposed a first-use defect: the intermediate Driftctl state directory was `0775` when `verify` ran before `inspect`. The CLI now protects that directory before artifact creation, with a subprocess regression. | Kept the installation path and permission fix. Package-registry and Homebrew distribution remain release work, not prerequisites for the local MVP boundary. |
| Synthetic long-context injection spike | Tested whether App Server `thread/inject_items` could create a fully reproducible long native checkpoint without source-agent turns. | Three injected raw Responses items were accepted as model-visible history, but `thread/read` returned zero turns and no importable user records. | Rejected as a replacement for real source turns. It may only add non-authoritative context between real goal and steering turns; the importer must ground intent in native user records it can read. |
| Native long-session runner | Added one-case orchestration that records two actual task/steering user turns around optional non-authoritative context, invokes the paired compare entrypoint, and runs every existing external verifier separately on both retained child candidates. | Fake App Server/CLI E2E covers two immediately interrupted source turns, accepted and unsupported injection, equal compare dispatch, six per-arm verifier calls, scoreable arm files, and private locator retention. Initial integration found two factual defects: the optional private locator artifact used default permissions, and the runner read `elapsed_millis` while the real verifier emits `elapsed_ms`. It now creates unique `0600` artifacts under `0700`, records the real field, labels pair-wide time as such, and exposes no raw provider rejection text. The source thread requests Luna Max, workspace-write sandboxing, and no approval escalation; at this stage the runner incorrectly assumed both forks would inherit that policy. All nine evaluator tests pass. | Kept for one live case before any five-case run. Results are explicitly `native_long_session` and descriptive-only; unsupported injection cannot support a long-context-pressure claim. |
| First native live pilot | Ran case 01 with Luna Max and 128 KiB of accepted assistant-context injection through the real source-session, compare, and external-verifier boundaries. | The 262.061-second pair was invalid as efficacy evidence: both arms returned completed with no changed paths and failed all three verifiers, so both were correctly scored as premature completion. The source user turns themselves said “do not edit files,” leaving an authoritative planning-only instruction in both branches. The runner also removed its temporary candidate/state root after scoring, making the private locators non-resumable. The first corrected retry failed before a model call because Plan mode lacked the experimental capability. After enabling it, the source model issued `request_user_input`; the client ignored the server request and deadlocked until manually interrupted. The next no-source-model retry exposed a benign race: the turn became terminal after `turn/start` reported in-progress but before `turn/interrupt`, which returned `no active turn`. Those retries’ private run roots remain available outside Git. | Retained every attempt as infrastructure-invalid evidence, not selectively erased or counted. The runner no longer uses Plan mode or asks a source model to interpret the task. It records each task-only user turn through native `turn/start`, immediately interrupts any in-progress source turn, accepts only the observed already-terminal interrupt race, and fails closed on every other App Server error. Source cleanliness is rechecked before comparison. When `--artifacts` is supplied, the private source workspace, state, candidates, and locators persist under a unique `0700` run root. |
| Inactive compactor fields | Inspected the retained artifacts after the next case-01 retry exhausted its one repair and failed `invalid_compactor_proposal`. | Both Luna proposals had the correct goal, one valid `add` clause, complete source accounting, and no active semantic disagreement. They differed only by populating `target_key` and `intent_keys`, fields that have no meaning and are never read for `add`. | Initial `add` validation now deterministically ignores those two inactive fields while retaining strict key, text, source, alternative, transition, and authority checks. A focused unit test uses the observed shape; a process test still rejects and redacts semantically active invalid alternatives. |
| Forked worker policy reset | Reached the first fully retained case-01 pair after fixing source seeding and compactor shape handling. | Both arms completed in a 173.220-second pair, changed no files, and failed all three verifiers. Their private trajectories showed the reason: both forks ran Sol High with a read-only sandbox, despite the source thread requesting Luna/workspace-write. The agents explicitly reported that they were blocked by read-only workspaces. This disproved the emitted `inherited_from_parent` policy claim. The first corrected live retry then stopped before child work because Codex requires the App Server client to advertise `experimentalApi` before `thread/settings/update`. The next stopped because the settings notification did not arrive before an unrelated follow-up response, disproving an ordering assumption. The first writable pair took 293.179 seconds and both arms passed all three verifiers, but private turn contexts showed Luna High rather than the reported Max: the global App Server default overrode the stored and per-turn Max setting across the compacted fork. | Retained but did not count any invalid attempt or the High-effort tie as Luna-Max efficacy evidence. Fork, resume, and turn start establish the same explicit model/effort/workspace-write/never-approve policy on both arms. The short-lived child-control connection advertises the required experimental capability and pins its own reasoning default to the requested worker policy; it does not enable Plan mode or alter the source session. After updating settings, Driftctl verifies model, effort, sandbox, approval policy, child ID, and child CWD from the documented effective fields returned directly by `thread/resume`; notification timing is irrelevant. Public output reports the verified policy instead of claiming inheritance. |
| First valid native pair | Repeated case 01 after pinning the child App Server default, using the same source task, late steering, 128 KiB accepted context injection, isolated starting trees, Luna Max workers, tools, sandbox, and external graders. | Private turn contexts confirm both actual worker turns used Luna Max, `never` approval, and workspace-write access only to their respective candidate roots. The source remained clean and unchanged. Both arms changed `service_client.py` and `tests/test_unit_client.py`, passed unit, integration, and combined graders, and avoided premature completion. Paired wall time was 534.048 seconds; the implementations were not byte-identical. | Valid parity result, not evidence of benefit. Retained without rerunning a preferred arm. Case 01 is too easy for this checkpoint/model because the baseline recovered the late steering from native context; later cases must test whether the bounded projection helps under more discriminating supersession, protected-boundary, interruption, or rename pressure. |
| Private eval temp boundary | Started frozen case 05 after the valid case-01 tie. | The first attempt stopped before agent work when Git hit the `/tmp` tmpfs quota while creating a candidate. Moving only the retained run root to `/home` exposed the remaining leak immediately: the semantic compactor still used inherited `/tmp` for its schema/output files and failed before producing an artifact. | Kept both attempts as infrastructure-invalid. Every retained native run now creates a mode-`0700` temp directory inside its own private root and passes it as `TMPDIR` to comparison and verification subprocesses; a fake-process E2E asserts the real subprocess environment. Existing evidence was not deleted. |
| Durable source-turn checkpoint | Completed the first case-05 process run after fixing the temp boundary. | Both arms passed unit tests but failed integration because neither implemented the late `submit_payload`/`resume_payload` steering. Inspection then showed the projection and source cursor contained only one user record. The source rollout proved why: the second `turn/start` returned an ID, but its immediately interrupted user message had not persisted before the seeding App Server was closed. The runner had counted returned IDs instead of imported records, so both case 01 and case 05 were invalid as long-session efficacy evidence. | Retained their process, policy, permission, and grader observations but revoked efficacy validity. After each seeded user turn, the runner now polls native `thread/read` and requires exactly one, then exactly two, durably readable `userMessage` items before comparison. A fake-process failure case returns two turn IDs while withholding the second message and must fail closed. |
| Native read startup race | First live run with the durable source-turn checkpoint called `thread/read` while the new rollout file was still empty. | Codex returned an internal read error containing the private rollout path, and the runner echoed that provider text in its public JSON error. No comparison or agent work ran. | The bounded durability poll now retries native read rejections and exposes only a stable `provider rejected request` reason. The special already-terminal interrupt race remains explicitly recognized without rendering arbitrary provider text. A fake native read returns a private-path error once and the runner must recover without exposing it. |
| Durable turn ordering | Retried case 05 with sanitized read retries. | Interrupting first can abort a turn before even its first user message is committed; polling afterward cannot recover a record that never existed. A direct schema probe also showed that raw injected user messages are model-visible history but do not appear as native `userMessage` turn items, so using injection would create false importer evidence. The first pre-interrupt implementation still failed because its one-second durability window was shorter than observed App Server startup; a read-only probe measured the user item becoming visible after about 1.9 seconds. | Source turns now run in a read-only source workspace. Driftctl waits up to five seconds for the exact native user-message count, interrupts the active turn immediately, then verifies the same count again. This allows no source writes, does not elevate injected context to user authority, and fails before child usage if either task or steering is missing. |
| Exact mutation-scope gate | Ran case 05 after both source turns became durable. | Both Luna Max arms passed unit, integration, and combined graders, but the workflow also created unrelated `tasks/plan.md` and `tasks/todo.md`. The runner still marked it verified because it scored only agent status and graders. | Retained the pair as a diagnostic pilot, not a scored result. Every case now declares exact allowed changed files; baseline, workflow, native runner, and scorer fail completion on any extra path. Missing or malformed scope evidence fails closed. Case 05 permits `work_queue.py` because queue-state behavior is part of the task. |
| Scoped native case-05 pair | Repeated case 05 once under the new fingerprint and exact mutation-scope gate. | Both actual Luna Max arms completed, passed unit, integration, and combined graders, and changed exactly `scheduler.py`, `work_queue.py`, and `tests/test_unit_scheduler.py`. Source state remained clean; paired wall time was 593.314 seconds. | Valid parity result with no significance or efficacy claim. Keep it as evidence that the native process, worker policy, external grading, and scope gate work; redesign the remaining cases before spending usage on more simple pairs. |
| Post-steering context pressure | Audited whether the native pairs actually placed long-session pressure after the critical instruction. | The runner injected all synthetic assistant activity before late steering, leaving that steering tail-salient. The valid native pairs therefore proved process behavior and parity, not resistance to context pressure or native compaction. A fresh real App Server rehearsal then durably recorded two ordered user turns, accepted four post-steering chunks totaling 4,096 bytes, and left the private source Git workspace clean. | Preserve the earlier results with narrower labels. The runner now records every steering update as its own ordered native user turn, then injects four varied non-authoritative assistant chunks after the final steering and reports their position and exact byte count. Do not claim native compaction without an observed provider boundary. |
| Chronological supersession case | Reworked the pagination checkpoint after the simple one-update cases produced native parity. | The prior case had one tail-salient additive instruction and duplicate values were identical, so first-seen and last-seen implementations could produce the same output. After using distinct later values, a known-good first-seen implementation passed unit and held-out checks three times; a known-bad last-seen implementation failed the intended held-out assertion while passing caller immutability. | Case 02 now records a last-occurrence duplicate policy and then an explicit final correction that supersedes it with first-seen order plus caller immutability. Keep the corrected grader and freeze its new fingerprint before live use. |
| Initial supersede inactive fields | Started the first post-steering-pressure case-02 pair. | The run stopped before either coding arm after both Luna proposals correctly added the old duplicate policy, superseded it with the final policy, and preserved filter immutability. Both populated `intent_keys` on `supersede`; the schema requires that field but the transition uses only `target_key`, so the validator exhausted its repair on semantically inactive data. | Retained the failed run as infrastructure-invalid. Initial `supersede` validation now ignores `intent_keys` while still requiring a valid target, unique replacement key, nonempty clause, legal transition, source authority, and no alternatives. The observed shape has a failing-then-passing regression; formatting, strict Clippy, and the full Rust suite pass. |
| Explicit case scope | Scored the corrected post-pressure case-02 pair with exact mutation scope. | Baseline passed. Workflow passed every functional grader but created `tasks/plan.md` and `tasks/todo.md`, so scope correctly rejected it. The private trajectory showed the structured projection triggered the existing planning skill, which mandates those files. The case had never told either agent that only two paths were authorized, making the hidden penalty unfair. | Retain the negative pair but do not count it under the revised fingerprint. Every case now states its exact allowed files as an initial user requirement shared by both arms. Existing harness instructions remain unchanged; the external scope gate tests compliance with an instruction the agent actually received. |
| Eligible scoped supersession pair | Repeated case 02 after making its path boundary agent-visible in both arms. | With three ordered user turns and 128 KiB of accepted post-steering activity, both actual Luna Max arms passed unit, held-out integration, combined graders, and exact two-file scope. Source state stayed clean; paired wall time was 366.687 seconds. | Valid parity and no-harm evidence only. Do not claim benefit from this case. Use one declared higher-pressure point before deciding whether more simple pairs are informative. |
| Higher-pressure parity | Repeated the same frozen case 02 at 512 KiB of accepted post-steering activity. | Both arms again verified with exact scope in 428.536 seconds. Private token telemetry peaked near 177,000 input tokens against Luna's locally reported 258,400-token effective window, and no compaction event was observed. | Valid stress parity, but not a compaction test. Raise only the evaluator's bounded synthetic-pressure ceiling from 512 KiB to 1 MiB, with a process regression rejecting larger input; run one approximately 800 KiB point and stop escalating. |
| Native-compaction parity | Ran the one declared 800 KiB case-02 stress point. | Both child rollouts recorded native `compacted` and `context_compacted` events after peaking near 240,000 input tokens, then continued near 32,000 baseline and 27,000 workflow input tokens. Both arms passed unit, held-out integration, combined graders, and exact scope; paired wall time was 528.933 seconds. | Valid compaction-boundary parity. Stop escalating pressure. This case proves the workflow survives real native compaction without regression, not that it improves coding correctness. Retain the negative efficacy result and seek any benefit only in different failure modes, not by rerunning this case. |
| Unattended conflict gate | Added a separate safety acceptance case in which three native user turns leave JSON versus YAML explicitly unresolved, then invoke the installed `continue` entrypoint with no TTY. | The first live run preserved source state but exited `1`: both Luna Max proposals made the correct decision yet violated an undocumented conflict shape, and the generic repair prompt repeated the mistake. The contract now requires an empty conflict target and an earlier active intent key. One frozen rerun resolved in one call (62.159 seconds; 13,699 input, 3,251 output, 2,944 reasoning-output tokens), exited `2`, emitted two source-linked alternatives, created no child/candidate, kept private state at `0700`/`0600`, and left the source session, native goal, Git HEAD/status, and worktree list unchanged. | Kept as one real no-TTY safety acceptance result: `unsafe_continuation_blocked = true`. It proves fail-closed behavior for this conflict, not coding-quality improvement or statistical significance. |
| Checksum installer | Packaged the current host release binary into a versioned archive and added a version-pinned local installer. | The release test built the optimized binary, generated a SHA-256 sidecar, installed through the script into a fresh prefix, and ran the installed `--help` entrypoint. A copied archive with appended corruption was rejected before any binary was installed. RustSec scanned all 26 locked dependencies and reported no known vulnerabilities. | Kept as a working local release boundary. Public release artifacts, license selection, additional target binaries, and a real Homebrew install remain open and are not claimed. |
| Continuation evidence handoff | Made the successful `continue` response expose candidate changes, current evidence, and blockers instead of leaving verification status implicit. | A fresh installed-binary run continued one disposable real Codex parent in 36.1 seconds. It returned the resumable child/CWD, no changed paths, no evidence, and `external_verification_required`. The public `verify` entrypoint then passed a requirement-specific check in 7 ms with equal candidate-before/after digests. Independent parent session, native goal, source HEAD/status, and worktree snapshots matched before/after. | Kept. Provider completion remains a process fact; verified adoption remains explicitly blocked on external evidence. |
| Frozen case calibration | Replaced prose-only case confidence with frozen gold projections, information-matched flat summaries, reference overlays, known-bad overlays, and complete case-input fingerprints. The first gold schema omitted the superseded case-02 rule and stable requirement IDs, so it could not score leakage or map evidence safely; closed-world actives, explicit inactive intent, non-identity provenance labels, and per-requirement evidence mappings were added before live use. | All five reference candidates passed unit, hidden integration, and combined verifiers in three independent temporary copies. All five known-bad candidates passed the visible unit boundary and failed the intended hidden integration boundary. The calibration uses no model, network, or paid call. | Kept as deterministic case-quality evidence. It does not substitute for live A/B/C outcomes. |
| Neutral source bundles | The calibrated cases had frozen workspaces and task contracts but no portable transcript-shaped input, making independent back-testing unnecessarily harness-specific. | Manifest generation now derives one schema-v1 synthetic Codex bundle per case from the exact goal, ordered user steering, and workspace fingerprint using the shipped record/source digest algorithms. Verification checks every byte and all five calibration cases pass through the public calibration command. | Kept. Bundles contain no private sessions or native goal state and do not claim to reproduce model behavior by themselves. |
| Native evaluation fail-closed checks | Required the A/C runner to validate the CLI's equal-manifest, equal-prompt, worker-policy, parent, and source claims; added one optional flat-summary B fork, strict text/provenance fidelity, per-requirement evidence records, and separate projection-generation accounting. | Fake-process E2E rejects unequal manifests and superseded-intent leakage, exercises persisted B fork, child-only goal read-back, pinned Luna Max App Server default plus policy read-back, terminal notification wait, external verification, exact scope, and source-message-count preservation. Pre-live comparison against the shipped CLI contract also caught and corrected a fake-only `ready` status; real inspect emits `usable`. The scorer accepts B as a distinct arm. | Kept for the frozen flagship control. At this stage review was still `not_evaluated`; no live B result exists yet, and deterministic runner tests are not efficacy evidence. |
| Independent completion review | Added one arm-blind acceptance review after each A/B/C candidate because process success, graders, and scope alone do not satisfy the frozen completion predicate. | Fake-process E2E covers pass, Required finding, incomplete coverage, candidate mutation, private `0700`/`0600` artifacts, and public rationale redaction. The first acceptance pass found that reviewer sessions were persisted and unbounded; they are now ephemeral and killed after 600 seconds, with partial output retained privately and completion failed closed. A fresh-prefix live case-01 rehearsal then crossed the real Luna Max/read-only/never-approve boundary: both arms passed all graders and exact scope, but review found one Required defect in the workflow arm's configurable retry loop that the frozen tests missed. Baseline verified; workflow did not. | Kept as a one-pass gate with no feedback/fix loop. The 794.858-second rehearsal is diagnostic evidence that the review can change closure, not evidence that Driftctl improves efficacy. Raw output remains private at `0700`/`0600`. |
| Structural fidelity correction | The same live rehearsal scored projection fidelity 0/3 because the evaluator required exact source wording even though the resolver contract explicitly synthesizes concise clauses. | The retained projection expressed all three requirements with different wording. A deterministic v2 scorer now maps fixture provenance labels to the exact ordered native record IDs, distinguishes the co-sourced scope item by kind, enforces closed-world active counts, and detects active use of an inactive/superseded source. Text equality is diagnostic only. The retained live projection back-tests at 3/3 structural matches, zero unexpected items, and zero inactive leakage with 0/3 verbatim clauses; a superseded-source leakage fixture still fails. All 36 evaluator tests pass. | Kept the v2 structural/provenance metric and retained the original failed-fidelity result unchanged. Do not describe v2 as a semantic judge; behavioral graders and the independent reviewer remain the correctness evidence. |
| Frozen long-context pressure | The diagnostic reviewer rehearsal used only 32 KiB after steering, which was enough to exercise the process but too weak to carry the main long-session framing. Running 800 KiB on every A/B/C arm would repeat an already-observed compaction parity point at high weekend cost. | The final five-case manifest now freezes 128 KiB of post-steering non-authoritative context for every arm. The earlier case-02 800 KiB run remains separate evidence that both native and projected arms crossed real compaction and still passed. | Kept 128 KiB as the common five-case pressure and 800 KiB as a separate no-harm boundary. Neither setting implies statistical significance. |
| Frozen five-case suite | Ran the five cases once in frozen order at 128 KiB and retained every arm and failure. | Four cases reached coding comparison. Baseline verified 3/4 and workflow 2/4. Case 01 failed both arms after review found the once-retry configuration gap; case 02 was A/B/C parity; case 03 favored baseline because review caught the workflow's skipped empty CSV row; case 04 was A/C parity. Case 05 stopped before coding with a source-linked three-alternative conflict over contradictory dual identifiers. | Retained as negative efficacy evidence: no measured Driftctl improvement. Treat case 05 as a safety/intervention outcome, not a completion result. Do not selectively rerun any arm. |
| Projection expansion and safety classification | The first structural scorer still assumed one native clause per gold requirement, so it failed cases 02–04 when the compactor split one requirement into several source-linked clauses. The suite also labelled case 05 infrastructure-invalid because any nonzero `inspect` was treated as a crash. | Structural/provenance v3 groups one or more clauses by exact ordered native source IDs and unambiguous scope kind, rejects unexplained or inactive-source items, and leaves text equality diagnostic. All five retained projections back-test at 3/3 requirements with zero unexpected items or inactive leakage. A cached installed-CLI read proved case 05 was `blocked`, with one conflict and three source-linked alternatives; it made zero model calls. Deterministic E2E now reports `safety_blocked`, starts no coding arms, and distinguishes it from infrastructure failure. | Kept the original suite output unchanged and added the corrected derived interpretation. V3 is structural, not a semantic judge. The new safety classification is process-tested and cached-boundary-tested; no model rerun was used to obtain a preferred label. |
| Post-turn source attestation | A completion audit found that workspace equality was checked only while candidates were created. After child execution, `compare` and `continue` rechecked the provider session but emitted `source_unchanged: true` without re-reading the source worktree. | Two subprocess regressions make the fake child alter the source's tracked README during `turn/start`. Both public entrypoints initially returned exit `0` and falsely reported preservation. They now recapture the exact pre-isolation manifest after the terminal child turn and exit `2` on mismatch. The same two cases pass against a checksum-installed release binary, not only Cargo's test binary. | Kept as a required safety fix. The source is not repaired or deleted; the operator receives a blocking result, and no success attestation is emitted. |
| Complete parent transcript attestation | The parent preservation check compared only user messages and the native goal, so a changed assistant, tool, or compaction item could still produce `parent_unchanged: true`. | A process regression changed only one persisted assistant item between the pre/post App Server reads; `inspect` initially accepted it. The importer now retains the complete private thread value in memory for equality while still granting intent authority only to explicit user text. Installed-binary tests prove `inspect`, `compare`, and `continue` reject the non-user mutation. | Kept. The full thread is neither rendered nor added to public state; the source-linked intent projection remains user-authoritative. |
| Operator conflict resolution | The unattended conflict gate could detect ambiguity but offered no production path to resolve it, so the same session could never continue through Driftctl. A domain regression also showed that selecting an alternative reactivated the vague conflicted clause instead of the selected text. | Resolution now copies the chosen alternative into the active frontier, records a private source-linked operator approval, and rebuilds the cached projection without another model call. Non-TTY use exits `2` with the exact `--resolve-conflict` command; an invalid alternative exits `1` before any fork; a valid choice starts the child with only the selected instruction. The full path passed through a checksum-installed release binary. | Kept as an explicit human decision boundary. Driftctl never guesses among ambiguous alternatives, and resolving one conflict does not silently resolve any other conflict or native-goal decision. |
| Run-bound requirement evidence | The public verifier produced a standalone result that could not affect the continued run, so every continuation remained permanently blocked regardless of passed checks. | A failing process test first confirmed `verify --run` was unsupported. `continue` now privately binds the exact isolated child; a passing bound command appends source-linked evidence to one active requirement and updates the durable projection. After the candidate was changed, a failing check invalidated that evidence and reopened the requirement. The pass, mutation, and reopen sequence passed through a checksum-installed release binary. | Kept as the causal requirement-evidence layer. Standalone `--candidate` checks remain non-authoritative, and requirement completeness is not labelled full verified completion until regression, integration, protected-scope, goal-alignment, and independent-review gates are connected. |
| Candidate-bound closure predicate | Requirement evidence alone could make the semantic projection closable even though regression, integration, protected scope, and independent review had never run. | The run now stores private results for four explicit gate commands. Missing gates blocked and all four passing on one candidate digest produced `verified_completion: true`. After the candidate changed, a failing review reopened stale requirement evidence, the earlier gates became stale, and a passing review retry on that unchanged checkpoint was rejected before execution. The full sequence passed through a checksum-installed release binary. | Kept. Provider completion is still only a process fact. Review is one-shot per candidate checkpoint with no automatic fix loop, and its command must fail on unresolved `Critical` or `Required` findings. |
| Manual child-goal handoff | A missing or partial App Server goal capability returned only a generic error after creating the isolated child, leaving the operator without a safe continuation path. | A goal-set capability failure now rechecks the parent, leaves the child turn unstarted, and emits the exact child ID/CWD, observed and intended states, `codex resume` argv, and separate `/goal clear` plus `/goal` instructions. The JSON handoff requires new approval and passed through a checksum-installed binary. | Kept as a blocked manual fallback, not automatic recovery. Provider identity or parent-preservation failures still return no handoff. |
| Neutral bundle intake | The strict cross-harness bundle existed only as a Rust library type; the shipped CLI could export a projection but could not consume another harness's local session snapshot. | `inspect bundle` now accepts a 16 MiB-bounded file or stdin document, validates schema, roles, record/source digests, and canonical repository identity before model use, then produces durable projection state. Cached file and stdin reinspection made no new model call; wrong-repository and future-schema inputs failed before model use. The sequence passed through a checksum-installed binary. | Kept as the portable seam without adding unproven native adapters. Updated snapshots require a new `session_ref` in this release; native continuation remains Codex-only. |
| Long-session provider compaction | The Codex importer dropped every non-user item, missed native compaction boundaries, and capped one App Server response at 1 MiB. Retaining raw tool output instead would make the continuity layer grow with the transcript. | A real local session returned 13,191,721 bytes, 5,703 items, 40 user messages, and 16 `contextCompaction` markers. Installed-binary rehearsal exposed, in order, an invented non-empty web-search rule, mismatched initial/incremental chunk sizing, and non-user-only chunks that could not carry intent authority. The final run validated the real App Server response, retained every item ID/role/digest privately, coalesced non-user evidence, needed one deterministic semantic call, re-read the parent, and exited `2` only for the expected native-goal conflict. A 2 MiB fake tool result also produced a prompt below 32 KiB; assistant-only append advanced the cursor with zero new model calls. | Raised the transport bound to 128 MiB only after a 101,487,379-byte organic response exceeded 64 MiB; a read-only rehearsal measured about 339 MiB peak RSS. Kept buffered read for the MVP and documented the bound instead of claiming streaming. Strict item validation, digest-only evidence batching, compaction counts, and user-only authority remain. The deterministic rehearsal proves transport/provenance, not model efficacy. |
| Organic default-model projection | Constructed cases did not prove that the default compactor could project an actual accumulated session. An inactive immutable snapshot supplied 42 authoritative user records without editing or manufacturing its history. | Attempt 1 ran 765.257 seconds and failed after two calls: the first proposal omitted one source and the blind repair invented two IDs. Prompt v2 then supplied the exact validator code, prior proposal, and canonical authority list without weakening validation. Attempt 2 ran 572.599 seconds: source accounting became exact, but four conflict operations had empty semantic keys and one supersession targeted an inactive predecessor. Tests captured both observed shapes and the prompt now states the missing key and active-chain contracts. Attempt 3 used the checksum-installed CLI and one Luna Max call (433.193 seconds; 34,220 input, 35,745 output, 29,977 reasoning tokens). It produced a validated projection and exited `2` on three conflicts, exact native-goal mismatch, and 27,822 bytes over the 16 KiB projection budget. | Kept all three private raw runs and the final blocked result. This is real organic model/provenance/fail-closed evidence, not coding-efficacy evidence: no continuation arm started. The measured goal-alignment and budget blockers remain open acceptance work rather than being relabelled success. |
| Manual-trial cutoff | The validated organic projection exposed two product-level usability blockers: exact native-goal rewording looked like migration, and a 27,822-byte active projection exceeded the 16 KiB default. Prompt v3 preserved a known native goal verbatim unless explicit steering changes it, and the default budget moved to 32 KiB. | The checksum-installed v3 attempt used two Luna Max calls over 991.576 seconds (74,535 input, 54,609 output, 42,650 reasoning tokens) and still failed `invalid_history_transition`. Both proposals covered all 42 sources and had valid keys, but tried to supersede an item already marked conflicted. A RED/GREEN process case reproduced the exact lifecycle. Prompt v4 now treats only end-state ambiguity as conflict and tells repair to collapse an ambiguity resolved by later explicit steering into final active intent with both sources cited. | Stopped provider reruns before the manual trial. V4 format and focused process tests pass, but it has no real-provider success claim yet. Constructed eval reruns, A/C comparison, Homebrew, and publication remain deferred until the operator proves the installed CLI useful on an inactive long session. |
| Evaluation fairness and scoring audit | The native runner checked checkpoint, manifest, prompt, model, sandbox, approval, and grader parity but did not require reported tool-policy or turn-timeout parity. The scorer also accepted internal workflow closure as completion evidence and rendered unavailable token/intervention data as zero. | Red tests reproduced all three gaps. `compare` now reports the shared tool policy and provider-terminal-event turn policy; the evaluator requires both and applies a 1,800-second outer subprocess timeout. Native completion now requires the independent review result, and internal closure is ignored. The scorer reports external acceptance, requirement rate, full-suite regression, projection-input fidelity, interventions, elapsed time, false-completion observations, and token availability; missing legacy data stays `null`/unavailable. Re-scoring the nine frozen arm files preserved the published result: baseline 3/4, workflow 2/4, plain summary 1/1. | Kept. Equal policy is not an enumerated tool-inventory proof, and the provider terminal-event policy is not a per-arm wall-clock deadline. Frozen artifacts remain unchanged; unavailable historical intervention, projection-arm, and token data were not reconstructed. |
| Honest release matrix and name collision | The installer advertised Linux ARM and macOS targets that had never been built, and `driftctl` was treated as a possible public name. | Red process tests showed unshipped targets reached Cargo or curl before failing. Packaging and installation now accept only the checksum-installed `x86_64-unknown-linux-gnu` target and reject all others before build/network access. Public-name research found an established Snyk `driftctl` CLI and an existing Homebrew formula with the same command. | Kept the proven Linux target. Homebrew and other targets remain blocked rather than simulated. `driftctl` remains an internal name until the operator selects a public name and license. |
| Final trust-boundary acceptance audit | The post-turn source check reused the candidate-copy manifest, so mutations to excluded `.codex`, `.claude`, `.cursor`, `.aider`, `.driftctl`, secret-named files, or hidden graders could pass while reporting `source_unchanged`. The child goal was also bound from its pre-turn read-back and never observed again before closure. | Red user-entrypoint tests mutated excluded `.codex/config.toml` during `compare` and `continue`, changed the child goal during its turn, and changed it externally before the final review gate. Source capture now streams protected tracked/ignored/untracked paths, type, mode, contents, and symlink targets into an opaque digest without copying or rendering them; `.git` remains excluded. A fresh App Server now re-reads each child goal after the turn and before every run-bound completion decision. The checksum-installed binary passed all 32 process tests, and a read-only real App Server probe successfully observed a persisted local goal through a fresh process without starting a model turn. | Kept both Required fixes. Host-wide/YOLO containment remains explicitly unclaimed, but protected source/harness mutation and stale observable-goal evidence now fail closed. |
| Recorded arm-order control | The frozen comparison always executed baseline before workflow, leaving a possible provider-order effect even though checkpoints and policies were equal. | A red installed-entrypoint test first showed `--arm-order` was rejected. `compare` now executes either requested A/C order and records it; the native runner enforces the reported order, and the five-case orchestrator alternates order by frozen case position. | Kept for future reproduction. The published 3/4 versus 2/4 arms remain untouched and explicitly baseline-first; no result was selectively rerun. |
| Pre-manual packaged-binary stress gate | Rehearsed the exact artifact intended for the operator trial instead of rerunning the constructed efficacy suite. | A fresh archive passed checksum installation and matched the existing manual binary byte-for-byte. Ninety-four Rust integration tests passed across the installed entrypoint, App Server subprocess protocol, malformed and large intake, conflict and goal blockers, interrupted-state recovery, writer locking, child isolation, source re-attestation, protected-path mutation, and verification. Four installer tests, strict Clippy, formatting, shell checks, and diff checks passed. The installed binary then reopened a real inactive Codex session: one non-user-only cursor advance used zero resolver calls; the next reopen was idempotent, used zero calls, created no compactor artifact, and left the worktree unchanged. | Passed for operator testing. This proves packaging, native intake, cache, and fail-closed process behavior; it does not prove prompt-v4 projection quality or a full long-session `continue`, which remain the manual acceptance boundary. |
| Private-release safety gate | Ran one production-shaped installed `inspect -> continue` pipeline rather than another efficacy evaluation, then requested a release-blocker-only Daybreak review. | The real isolated child completed in 25 seconds; parent session and source worktree stayed unchanged, adoption remained manual, and missing evidence blocked verified completion. A candidate-bound protected-scope command then passed without closing the run. Daybreak found two Major local-disclosure defects: compactor temporary files and legacy raw trajectories were created with public modes, and the legacy trajectory directory followed a repository symlink. RED tests reproduced all three conditions. Temporary directories/files now start at `0700`/`0600`; legacy trajectories do the same and reject symlinked storage. The final gate passed strict Clippy, formatting, shell checks, 112 Rust tests, and four installer tests. | Fixed only the two release blockers and stopped the review loop. Package as a private Linux MVP. Claim local-only architecture and targeted security-review evidence, not an absolute security guarantee or proven semantic improvement. |
| Prospective paired A/B | The earlier `compare codex` ran two tool-created turns; it could not measure an organic baseline against a plugin-managed continuation. Added `ab prepare codex` for equal idle persisted forks and `ab report` for post-run verification and usage evidence. | Process E2E covers lineage, equal checkpoints, exact enrollment, partial failure, invariant rejection, one verifier per arm, mutation blocking, cache, and a workflow-only win. A disposable real checkpoint produced two Codex forks without plugin installation: baseline stayed detached, workflow used exact `$driftctl on`, both passed the same verifier, source stayed unchanged, and repeat report was cached. | Kept. The real rehearsal was `tie_pass` with asymmetric interventions, so it proves the pipeline—not efficacy. The frozen 3/4 versus 2/4 result remains unchanged. |
| Real A/B failure corrections | The real workflow blocked correctly on a goal replacement, then exposed an underspecified withdrawal shape; a full-suite stress run separately exposed nondeterministic advisory-lock release. | The withdrawal failed once after repair, received an exact proposal contract, and succeeded on the same persisted fork. Writer-lock release failed once in 7 stress runs before explicit unlock and passed 25/25 afterward. One `codex exec resume` also started read-only when the disposable driver ignored stored config; the retry supplied the recorded workspace-write policy. | Kept the two product fixes. Recorded the driver retry as an intervention rather than treating the rehearsal as controlled or rerunning it for a preferred result. |
| External A/B verifier binding | A single Daybreak release-blocker pass reproduced two different candidate-local scripts passing with equal command/verifier digests because a relative program was resolved separately in each arm. | A RED process test reproduced the false `same_verifier_command`. Report now canonicalizes one regular verifier program from the invocation directory, rejects candidate-resolved programs/inputs, hashes external file content, pins the digest before the second arm, and blocks mutation. The 40-case A/B/process file passed after the fix. | Fixed the sole Major finding. `v0.4.1` supersedes the withdrawn private `v0.4.0` artifact; no iterative review loop was started. |
| Historical A/B checkpoint | The first A/B command could fork only the complete stored thread, so an organic session could not be branched from the same earlier point. Codex supports `thread/fork.lastTurnId` but rejects an active selected turn; conversation history also cannot reconstruct old uncommitted files. | RED process cases first failed on the missing CLI. The final path preflights `status=completed`, reports the preceding completed turn on an active selection, resolves one explicit Git ref, creates equal commit snapshots, sends the same `lastTurnId` to both persisted forks, verifies the exact prefix, and reports against that prefix. The checksum-installed v0.5.0 binary then selected turn 1 of a disposable two-turn real Codex thread: both idle children contained exactly turn 1, omitted turn 2, started from the bound commit, and left the source unchanged. The in-progress process case invoked no semantic worker and created no experiment, workspace, or fork. A focused release-blocker security pass found no Critical or Major issue in the ref, checkout, thread, evidence, or package boundaries. | Kept as an explicit historical checkpoint mode. Current-checkpoint behavior is unchanged. Native goal provenance is labelled current-at-prepare, and uncommitted historical reconstruction remains unclaimed. |
| macOS organic A/B incident | A real prospective pair created equal candidates, but Codex rendered `$driftctl-codex:driftctl on`; the literal-only hook ignored it while the skill falsely claimed success. Both children then followed inherited absolute parent paths, and baseline `apply_patch` modified the parent despite candidate-only sandbox context. | Captured evidence showed workflow externally detached, both candidates clean, and the parent changed. I01 first reproduced missing durable enrollment; exact literal and Codex-qualified on/status/off now share one strict parser, and the skill cannot infer success without hook context. I02 first reproduced unsafe prepare reaching two forks; prepare now detects parent-checkout paths in selected provider content/native goal and exits `2` before A/B state, candidates, or forks. The release-blocker gate then found nested tool `cwd` values and equivalent path spellings bypassed the first scanner. RED cases reproduced both; the final scanner skips only the provider item's structural `cwd` and covers normalized, symlinked, shell-escaped, and case-variant paths. Safe prospective and historical process cases remained green. | The run is invalid evidence, not an outcome. Kept the fail-closed fixes for v0.5.1 and stopped after the single security gate; semantic efficacy remains unproven. |
| Click client-side A/B | Reconstructed the path-neutral post-investigation Click 8.2.1 checkpoint from the retained macOS incident, then used the released v0.5.1 binary and public Driftctl/Codex commands for an equal prospective pair. | Baseline changed three allowed files and passed the external verifier plus the full upstream suite. Workflow correctly blocked an inherited-goal conflict before implementation, but the required `/goal` handoff changed the source and made approval stale; the candidate remained clean and failed verification. Built-in reporting also rejected public CLI attachment because no literal `$driftctl on` appeared in the transcript. | Recorded baseline 1/1 versus workflow 0/1. Keep the safety block; treat native-goal approval recovery, SessionStart hook output, and activation/report compatibility as product blockers. Do not claim an efficacy gain. |

## Main failure mode

A structurally faithful projection does not guarantee better code. In the frozen suite, native history already preserved enough intent for strong Luna workers, while both modes still produced defects beyond the fixed graders. Ambiguous steering must block for operator input rather than be compressed into false certainty.

## Hot take

The projection was not the measured win; refusal was. More context and more reasoning did not improve verified completion in these cases. The useful reliability gain came from external causal evidence, an independent one-pass review, and a source-linked operator block when intent was genuinely ambiguous.
